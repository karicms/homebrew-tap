# homebrew-tap

karicms 的 Homebrew tap。

## 可用软件

### rename-draft

文件夹与文件分级编号重命名工具。

```bash
brew install karicms/tap/rename-draft
```

安装时会检测本机是否已有 `python3`：有则跳过，没有才自动安装 Homebrew 的 Python。

项目主页：https://github.com/karicms/rename_draft
